import { Button } from "react-native";
import Page from "./Page";

const ButtonComponent = () => {
 const handleClick = () => {
  <Page/>;
 } 
  return (
    <Button title="Login" color="#0000ff" onPress={handleClick}/>
  );
};

export default ButtonComponent;
